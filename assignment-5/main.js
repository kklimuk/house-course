(function() {
	'use strict';

	/*
	Parameters:
	- friends: Array with the format of input.json
	*/
	window.getConnections = function getConnections(friends) {
		// TODO: write your code here

		// The output should be of the format:

		// [{
		// 	first_name: 'name',
		// 	last_name: 'name',
		// 	one_in_common: [{
		// 		name: 'some name',
		// 		common: ['Cityville']{}
		// 	}, ...],
		// 	two_in_common: [], // same format as one_in_common
		// 	more_in_common: [] // same format as one_in_common
		// }, ...]

		// Check output.json for the unminified version of the output.


		return [];
	};

})();